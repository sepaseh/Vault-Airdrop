syntax = "proto3";

message Container {
    // version of data format
    uint64 version = 1;
    // vault contained the container
    string vault = 2;
    // is vault encrypted with password
    bool is_encrypted = 3;
}
